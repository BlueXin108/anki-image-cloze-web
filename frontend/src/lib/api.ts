import * as anki from '@/lib/anki-connect'
import * as preview from '@/lib/manual-preview'
import * as projectStore from '@/lib/project-store'

export const api = {
  anki,
  preview,
  projectStore,
}
